{
  "name": "screencrew-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "ScreenCrew desktop tray shell (Tauri). Wraps a self-hosted ScreenCrew server in a native window with a system-tray launcher.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  }
}
