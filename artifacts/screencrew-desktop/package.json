{
  "name": "lynxdock-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "LynxDock desktop shell (Tauri). Connects to a LynxDock server in a native window with a built-in server picker and a system-tray launcher.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0"
  }
}
